# Xcode / Swift
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside

# Swift Package Manager
.build/
.swiftpm/

# xcodegen が project.yml から生成(SSOT は project.yml)
*.xcodeproj

# Release secrets — never commit (Apple session, API keys, certs, profiles)
.env
.env.*
fastlane/.env*
*.p8
*.p12
*.mobileprovision
*.cer

# App Review 連絡先(PII — 公開リポにコミットしない。ローカルで fastlane に読ませる)
fastlane/metadata/review_information/first_name.txt
fastlane/metadata/review_information/last_name.txt
fastlane/metadata/review_information/email_address.txt
fastlane/metadata/review_information/phone_number.txt

# fastlane 自動生成物(SSOT は Fastfile / metadata)
fastlane/README.md
fastlane/report.xml

# Playwright MCP のスナップショットキャッシュ
.playwright-mcp/

# macOS
.DS_Store
